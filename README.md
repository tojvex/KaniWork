# Kani Group Web App

Interactive placeholder-first Next.js implementation for the Kani Group building navigation brief.

## Commands

```bash
npm install
npm run dev
npm run build
npm test
```

## Asset Workflow

The first implementation uses CSS/SVG placeholders. Real Figma exports can be added later by updating `assetRefs` in the floor data and placing files under `public/assets`.
